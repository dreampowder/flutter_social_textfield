name: flutter_social_textfield
description: TextEditingController and TextSpan builder with hashtag, url and mention detection using pre defeined and customizable regex patterns. Also supports tapping on the detected content.
version: 0.0.3
homepage: https://github.com/dreampowder/flutter_social_textfield

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
